/// Example environment configuration for the ULink SDK example app
///
/// Copy this file to `env.dart` and replace the values with your own.
/// Do NOT commit the `env.dart` file to version control.

class Environment {
  /// API key for the ULink service
  static const String apiKey = 'your_api_key_here';

  /// Base URL for the ULink API
  /// Use 'https://api.ulink.ly' for production or 'http://localhost:3000' for local development
  static const String baseUrl = 'https://api.ulink.ly';
}
